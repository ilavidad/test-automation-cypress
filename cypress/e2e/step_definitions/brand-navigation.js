const { Given, When, Then } = require("@badeball/cypress-cucumber-preprocessor");

Given("the user is on the home page", function () {
  // Step definition placeholder
});

Given("the user is on the products page", function () {
  // Step definition placeholder
});

When("the user clicks on a brand link", function () {
  // Step definition placeholder
});

Then("the brand page should be displayed", function () {
  // Step definition placeholder
});

When("the user filters by a specific brand", function () {
  // Step definition placeholder
});

Then("only products from that brand should be shown", function () {
  // Step definition placeholder
});
