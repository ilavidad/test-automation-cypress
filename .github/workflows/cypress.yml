name: Cypress E2E

on:
  push:
  pull_request:

jobs:
  cypress:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 24
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Create Cypress environment file
        run: |
          cat > cypress.env.json <<'EOF'
          {
            "validUser": {
              "email": "${{ secrets.OPENCART_TEST_EMAIL }}",
              "password": "${{ secrets.OPENCART_TEST_PASSWORD }}"
            }
          }
          EOF

      - name: Run Cypress tests
        run: npm run test
